#include <bits/stdc++.h>
using namespace std;

int main() {
	int n,p=0,t=0;
	cin>>n;
	int s[n];
	for(int i=0;i<n;i++)
	{
	    cin>>s[i];
	    if(s[i]==-1)
	    {
	        if(p==0)
	        { t++; }
	        else
	        { p--; }
	    }
	    else
	    {
	      p=p+s[i];  
	    }
	}
	cout<<t;
	

}
