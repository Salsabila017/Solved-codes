#include <bits/stdc++.h>

using namespace std;

int main() {
	int n,arr1[100],arr2[100],x,y,cnt=0;
	cin>>n;
	
    for(int i=0;i<n;i++)
	{
      cin>>arr1[i];
      cin>>arr2[i];
	}
	for(int i=0;i<n;i++)
	{
	    for(int j=0;j<n;j++){
	        if(arr1[i]==arr2[j])
	        {
	            cnt++;
	        }
	    }
        
	}
    cout<<cnt;
	
   

}
