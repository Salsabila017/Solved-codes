#include <bits/stdc++.h>
using namespace std;

int main() {
	int a,b,c,m,n,k,l,x,y,z,u,p,q,r,s,t;
	cin>>a>>b>>c;
	
	m = a+b*c;
    n = a*(b+c);
    k = a*b*c;
    l =(a+b)*c;
    x= a+b+c;
    y= a+(b*c);
    
    p=max(m,-1);
    q=max(p,n);
    r=max(q,k);
    s=max(r,l);
    z=max(x,s);
    u=max(z,y);
    
    cout<<u;

}
