#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin>>s;

        for(int j=0;j<s.size();j++)
        {
            if(s[j]=='H' || s[j]=='Q' ||s[j]=='9')
            {
               cout<<"YES"<<endl;
               return 0;
            }
        }
    cout<<"NO"<<endl;
    return 0;

}
